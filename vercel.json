{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo 'Static deploy — skipping dependency install' && exit 0",
  "buildCommand": "echo 'Static deploy — no build' && exit 0"
}
